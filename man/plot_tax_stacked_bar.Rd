% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_tax_stacked_bar}
\alias{plot_tax_stacked_bar}
\title{Plot Stacked Bar of Taxa Abundance}
\usage{
plot_tax_stacked_bar(
  exps,
  pheno,
  split_by_1 = NULL,
  split_by_2 = NULL,
  show_names = FALSE
)
}
\arguments{
\item{exps}{A data frame in long format (e.g., `outtable_long` from `tax_collapse()`), containing:
\itemize{
  \item The first column = taxonomic level (e.g., "genus", "species")
  \item A column `sampleID`
  \item A column `frequency` for relative abundance
}}

\item{pheno}{A phenotype data frame containing sample metadata. Must include `sampleID` column.}

\item{split_by_1}{Optional. A column name (string) in `pheno` for faceting by wrap. Default = `NULL`.}

\item{split_by_2}{Optional. A column name (string) in `pheno` for faceting by grid. Default = `NULL`.}

\item{show_names}{Logical. Whether to display sample IDs on x-axis (rotated 60°). Default = `FALSE`.}
}
\value{
A `ggplot` object representing the stacked bar chart.
}
\description{
Creates a stacked bar plot of relative abundance from taxonomic data, optionally split by
up to two grouping variables from phenotype data.
}
\examples{
\dontrun{
plot_tax_stacked_bar(exps = result$outtable_long, pheno = metadata)
plot_tax_stacked_bar(exps, pheno, split_by_1 = "Treatment", show_names = TRUE)
plot_tax_stacked_bar(exps, pheno, split_by_1 = "Treatment", split_by_2 = "Antibiotics")
}
}
